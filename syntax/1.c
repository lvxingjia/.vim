#include<stdio.h>
size_t FILE
#define LEN(x) kkd;
int main(void)
{
   , -1.23e-6.9
        , -8
        1.12lU
        0X7AF
       3.23e-21 j&&
        printf("kdk")
        1e-1
        3.2E-1,
       x - 3,-3
    return 0;//df
for(int i=0; i<7; i++) kk;
loop: goto loop 78
kkhaha : kkdf;
      printf()
ssysyy
if( (*(p+2)a(b())>)
if (abc(xyz))
    if(*(p)>*(p+1)&&*(i)<*(i++))
        if (ad(aa())>*())
#include<stdio.h>
\x8677 \'
#define LEN(arr) (sizeof(arr)/sizeof(arr[0]))

void swap(int *p1, int *p2)
{
    int tmp;
    tmp = *p1;
    *p1 = *p2;
    *p2 = tmp;
}
void selection_sort(int *p, int n)
{
    void swap(int*, int*);
    for(int i=0; i<n; i++)
        {
    int min_idx = i;
    for(int j=i+1; j<n; j++)
        if(*(p+j) < *(p+min_idx)) min_idx = j;
    if(min_idx != i) swap(p+min_idx, p+i);
        }
}
void bubble_sort(int *p, int n)
{
    void swap(int*, int*);
    for(int i=0; i<n-1; i++)
    {
        for(int j=0; j<n-i-1; j++)
            if(*(p+j)>*(p+j+1)) swap(p+j, p+j+1);
    }
}
int main(void)
{
    int lyst[] = {19, 5, 1, 0, 2, 3, -9, 4, 3, -1, 7, 2};
    int length = LEN(lyst);
    int *pp;
    void selection_sort(int*, int);
    printf("list: [");
    for(pp=lyst;pp<lyst+length-1;pp++) printf("%d ", *pp);
    printf("%d]\n", *pp);
    bubble_sort(lyst, length);
    printf("sort> [");
    for(pp=lyst;pp<lyst+length-1;pp++) printf("%d ", *pp);
    printf("%d]\n", *pp);
    return 0;
}
